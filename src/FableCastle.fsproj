<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <!-- Global to the app -->
    <Compile Include="Global.fs" />
    <!-- Info -->
    <Compile Include="Info/View.fs" />
    <Compile Include="Player\Types.fs" />
    <Compile Include="Player\State.fs" />
    <Compile Include="Player\View.fs" />
    <Compile Include="Game\Types.fs" />
    <Compile Include="Game\State.fs" />
    <Compile Include="Game\View.fs" />
    <!-- Counter -->
    <!-- Home -->
    <!-- Navbar -->
    <!-- App -->
    <Compile Include="Types.fs" />
    <Compile Include="State.fs" />
    <Compile Include="App.fs" />
  </ItemGroup>
  <Import Project="..\.paket\Paket.Restore.targets" />
</Project>
